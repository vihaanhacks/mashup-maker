---
description: Deploy the Mashup Maker to GitHub and Firebase
---

// turbo-all
1. Add all changes to the staging area.
`git add .`

2. Commit the changes.
`git commit -m "Deploy: Updated system time and synthesis error handling"`

3. Push the changes to GitHub.
`git push`

4. Deploy to Firebase Hosting.
`./firebase.exe deploy`
